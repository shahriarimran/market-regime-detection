[pytest]
testpaths = tests
addopts = --strict-markers
markers =
    unit: fast isolated contract tests
    integration: cross-module and frozen-artifact integration tests
    regression: frozen metadata and output regression tests
    slow: tests that load production data or model artifacts

